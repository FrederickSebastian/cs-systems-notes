1. 完美转发的`forward()`函数如下, 其本质还是用函数模板, 引用折叠和类型强转实现的:
```C++
template <class _Ty> 
_Ty&& forward(remove_reference_t<_Ty>& _Arg) noexcept 
{ 
	// forward an lvalue as either an lvalue or an rvalue 
	return static_cast<_Ty&&>(_Arg);
}
```

2. 可变参数模板有两种: 可变模板参数包可变函数参数包. 