# 小型高性能网络服务器框架：项目概述

这是一个典型的“从系统编程走向服务端架构”的过渡项目。  
目标不是做一个功能复杂的网站，而是：

- 建立完整的高性能服务器架构认知
    
- 将 Linux 系统编程知识串联起来
    
- 形成工程化服务端开发能力
    
- 为后续深入：
    
    - Reactor/Proactor
        
    - Nginx/Tomcat/Redis
        
    - RPC框架
        
    - 游戏服务器
        
    - 分布式系统
        
    - 网络中间件  
        打基础
        

你当前的知识储备已经足够开始。

---

# 一、项目目标

实现一个：

- 高并发
    
- 非阻塞
    
- 事件驱动
    
- 可扩展
    
- 模块化
    

的小型 Linux 网络服务器框架。

框架本身不局限于 HTTP。

后续可以在它上面挂：

- HTTP服务器
    
- 文件服务器
    
- 聊天服务器
    
- RPC服务
    
- WebSocket服务
    

因此：

> 重点是“框架”，不是“业务”。

---

# 二、项目最终效果（建议目标）

最终服务器应该具备：

## 基础能力

### 1. 高并发连接

支持：

- 数千 TCP 连接
    
- epoll ET模式
    
- 非阻塞socket
    

---

### 2. Reactor事件驱动

具备：

- accept事件
    
- read事件
    
- write事件
    
- close事件
    

事件分发机制。

---

### 3. 多线程处理模型

至少实现：

- IO线程
    
- worker线程池
    

结构。

典型模型：

```text
Main Reactor
    |
Sub Reactor
    |
Thread Pool
```

---

### 4. 定时器系统

支持：

- 连接超时关闭
    
- 心跳检测
    
- idle连接回收
    

---

### 5. 缓冲区设计

实现：

- 输入缓冲区
    
- 输出缓冲区
    

避免：

- 粘包
    
- 半包
    
- 频繁系统调用
    

---

### 6. 日志系统

支持：

- INFO
    
- ERROR
    
- DEBUG
    

最好异步日志。

---

### 7. 配置系统

支持：

```ini
port=8080
thread_num=4
log_level=debug
```

---

### 8. HTTP协议支持（建议）

至少实现：

- GET
    
- 静态资源响应
    
- HTTP解析
    

这样项目完整度会非常高。

---

# 三、推荐整体架构

建议采用：

## 主从Reactor + 线程池

架构：

```text
                +----------------+
                |   MainReactor  |
                | accept连接      |
                +--------+-------+
                         |
            新连接分发给SubReactor
                         |
      +------------------+------------------+
      |                  |                  |
+-----v-----+      +-----v-----+      +-----v-----+
|SubReactor1|      |SubReactor2|      |SubReactor3|
| epoll_wait|      | epoll_wait|      | epoll_wait|
+-----+-----+      +-----+-----+      +-----+-----+
      |                  |                  |
      +--------业务任务提交线程池------------+
                         |
                 +-------v--------+
                 | Thread Pool    |
                 +----------------+
```

这是现代高性能服务器常见结构。

类似：

- Netty
    
- Muduo
    
- Nginx（思想相近）
    

---

# 四、推荐模块划分

这是项目核心。

---

# 1. Socket模块

封装：

```cpp
class Socket
```

功能：

- 创建socket
    
- bind
    
- listen
    
- accept
    
- 设置非阻塞
    
- 设置TCP参数
    

目的：

避免大量裸 syscall。

---

# 2. Channel模块

核心思想：

> “一个fd对应一个Channel”

保存：

- fd
    
- 感兴趣事件
    
- 回调函数
    

例如：

```cpp
class Channel {
    int fd;
    uint32_t events;

    std::function<void()> readCallback;
    std::function<void()> writeCallback;
};
```

这是 Reactor 核心。

---

# 3. Epoller模块

封装：

```cpp
epoll_create
epoll_ctl
epoll_wait
```

---

# 4. EventLoop模块

最核心模块。

职责：

- 事件循环
    
- 分发事件
    
- 调用Channel回调
    

伪代码：

```cpp
while (!quit) {
    activeChannels = epoll.wait();

    for (channel : activeChannels) {
        channel->handleEvent();
    }
}
```

---

# 5. Acceptor模块

负责：

- 监听socket
    
- accept新连接
    

---

# 6. TcpConnection模块

非常关键。

表示：

> 一个TCP连接对象

保存：

- socket
    
- 输入输出缓冲区
    
- 状态
    
- 回调
    

---

# 7. Buffer模块

建议自己实现。

支持：

- append
    
- retrieve
    
- readFd
    
- writeFd
    

建议参考：

- Muduo Buffer设计
    

重点：

- 动态扩容
    
- 减少拷贝
    

---

# 8. ThreadPool模块

支持：

- 任务队列
    
- worker线程
    
- 条件变量
    

---

# 9. Timer模块

可选实现：

- 小根堆
    
- 时间轮
    
- 红黑树
    

建议先用：

```text
priority_queue
```

---

# 10. Logger模块

建议：

- 单例
    
- 异步写日志
    
- 宏封装
    

例如：

```cpp
LOG_INFO("client fd=%d connected", fd);
```

---

# 五、推荐技术路线（非常重要）

不要一次性全做。

---

# 第一阶段：单Reactor单线程

实现：

- epoll
    
- 非阻塞socket
    
- Echo服务器
    

架构：

```text
EventLoop
    |
 Channel
```

目标：

建立事件驱动模型。

---

# 第二阶段：Reactor封装

加入：

- Channel
    
- Epoller
    
- EventLoop
    

实现模块化。

---

# 第三阶段：TcpConnection

开始：

- 面向对象封装连接
    
- 缓冲区
    
- 回调机制
    

这是架构质变阶段。

---

# 第四阶段：线程池

实现：

```text
IO线程 + worker线程
```

避免业务阻塞IO。

---

# 第五阶段：主从Reactor

实现：

```text
MainReactor + SubReactor
```

这是高性能核心。

---

# 第六阶段：HTTP支持

实现：

- HTTP请求解析
    
- 响应构造
    
- 静态资源服务器
    

---

# 第七阶段：优化

例如：

## 网络层优化

- ET模式
    
- writev
    
- 零拷贝sendfile
    
- TCP_NODELAY
    
- SO_REUSEPORT
    

---

## 内存优化

- 对象池
    
- Buffer复用
    

---

## 并发优化

- 无锁队列
    
- CAS
    
- 线程绑定CPU
    

---

# 六、建议项目目录结构

```text
server/
├── base/
│   ├── Logger
│   ├── ThreadPool
│   └── Buffer
│
├── net/
│   ├── Socket
│   ├── Channel
│   ├── Epoller
│   ├── EventLoop
│   ├── Acceptor
│   ├── TcpConnection
│   └── TcpServer
│
├── http/
│   ├── HttpRequest
│   ├── HttpResponse
│   └── HttpParser
│
├── tests/
│
├── examples/
│
└── main.cpp
```

---

# 七、项目中的核心技术点

这个项目真正的价值在这里。

---

# 1. Reactor模型

必须真正理解：

- 事件驱动
    
- 回调机制
    
- IO复用
    

---

# 2. one loop per thread

现代服务器经典模型。

---

# 3. ET/LT区别

尤其：

```text
ET必须一次读完
```

否则死锁。

---

# 4. 非阻塞IO本质

理解：

```text
用户态/内核态
read为什么阻塞
epoll为什么高效
```

---

# 5. TCP粘包

HTTP解析时会深刻理解。

---

# 6. 生命周期管理

难点：

```cpp
shared_ptr
weak_ptr
```

TcpConnection 生命周期非常关键。

---

# 7. 回调设计

例如：

```cpp
std::function
bind
lambda
```

---

# 八、推荐开发语言

建议：

# C++17

因为：

- RAII
    
- 智能指针
    
- std::function
    
- thread
    
- mutex
    
- condition_variable
    

能明显降低复杂度。

---

# 九、推荐参考项目

非常建议阅读：

## 必读

- Muduo
    
- TinyWebServer
    
- WebServer
    
- Tinyhttpd
    

---

## 深入

- Nginx
    
- Redis网络模型
    
- Netty Reactor
    

---

# 十、最终项目成果建议

最终建议达到：

## 能展示：

### 1. 高并发HTTP服务器

支持：

```bash
ab -n 100000 -c 2000
```

压测。

---

### 2. 技术亮点

你的简历可以写：

- 基于Reactor模型的高性能网络框架
    
- epoll ET非阻塞IO
    
- 主从Reactor多线程架构
    
- HTTP解析与静态资源服务
    
- 线程池与异步日志
    
- 定时器与连接超时管理
    

---

# 十一、最关键的一点

你现在最应该做的：

不是追求“功能多”。

而是：

> 把服务器底层架构真正做扎实。

尤其：

- EventLoop
    
- Channel
    
- TcpConnection
    
- 生命周期管理
    
- 线程模型
    

这几个模块，决定你是否真正进入服务端开发。